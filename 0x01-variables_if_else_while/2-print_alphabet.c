#include <stdio.h>

/**
 * main - Prints the alphabetic
 * Return: Always (Success)
 */

int main(vooid)

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
        putchar('\n');
	return (0);

}
